import React, {Component} from 'react'
import {Typography} from "@ticketmaster/aurora";

const Results = ( props ) => {
    return (
        <div>
             <Typography.H4>
                (props.name)
            </Typography.H4>`

        </div>
    )
};

export default Results; 